{
  "name": "boolean-search",
  "version": "1.0.0",
  "description": "Implements boolean search mechanism via inverted index",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin J",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^4.0.1"
  }
}
